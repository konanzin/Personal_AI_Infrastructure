/**
 * UI System
 *
 * Material 3 design system adapted for PAI Mobile.
 * Tokens, wrappers, and visual patterns.
 */

// TODO(M2): Export theme tokens and hooks
// TODO(M5): Export message rendering components
// TODO(M5): Export card components (ToolCall, Permission, Question, Error)

export type UIComponent = 'text' | 'code' | 'tool_call' | 'tool_result' | 'permission' | 'question' | 'error';

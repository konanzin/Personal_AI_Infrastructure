# Drift Register — scaffolding com data de validade

Inventário de toda heurística/gambiarra que compensa fraqueza de modelo. Regra: **compensação sem linha aqui é defeito em review.** Origem: auditoria Bitter Lesson de 2026-07-05 (27 violações confirmadas, 30 rejeitadas como arquitetura legítima). Relatório completo: artifact `bitter-lesson-audit` (claude.ai/code/artifact/916e156c-d95d-47f1-90ef-5f6bbc66245c).

## O gate de 3 perguntas (para TODO componente novo)

1. Qual falha real, nomeável, este componente resolve?
2. Um modelo mais capaz o tornaria desnecessário? Se sim, qual probe detecta isso?
3. Ele é removível em horas (uma fonte, um commit)?

"Não" em qualquer uma = não mescla, exceto se for exceção enumerada: piso de segurança, budget de recurso, schema/contrato, pin de reprodutibilidade, guarda de irreversibilidade.

## Fase 1 (dossiês em PHASE1.md)

| ID | Mecanismo | Local | Proveniência | Mudança | Status |
|---|---|---|---|---|---|
| W1.1a | `skill_misfire` hard-deny por keyword | `pai-hooks.lib.js` (inspectSkillInvocation) | **port-added**, especulativo (sem incidente) | deny → warn/log | **feito** `7cba73a` — suíte 0 fail, guardas warn-nunca-deny |
| W1.1b | inspectPrompt deny + rewrite do prompt | `pai-hooks.lib.js` ~1129-1266, `pai-hooks.js` chat.message | corpus herdado do upstream (`PromptInspector.ts`); **rewrite é port-added** | deny/rewrite → alert; corpus vira telemetria | **feito** `61e3eb8` — suíte 511/0; piso 100%; prompt chega intacto + anotação advisory |
| W1.2 | "honor above self-selection" do classificador | `mode-classifier.lib.js` formatClassificationContext; `opencode.jsonc.template` | doutrina upstream v6.3.0 (incidente de sub-escalação, abr/2026) | classificação vira sugestão; `/eN` do usuário segue autoritativo | **feito** `a8beba6` — golden heurístico inalterado 68%/71%; golden LLM (gpt-5.4-mini-fast, runs=3): **98% mode / 100% tier**, igual ao baseline O3; único miss = caso borderline conhecido. Correções pós-auditoria (2026-07-06): `/eN` expandido pelo slash-command agora vincula (`6453d7c` — antes o branch override nunca disparava no caminho de comando); doutrina alinhada em **v6.3.3** (`38e0f38` — removidas as 3 ordens de obediência remanescentes); eval comportamental do executor criado (`47c8a6f`, primeiro probe under-correction 8/8). Golden LLM re-medido pós-fixes (2026-07-06, gpt-5.4-mini-fast, runs=3): **98% mode (55/56) / 100% tier (24/24)** — igual ao baseline, mesmo único miss borderline PT-BR; rito de medição do `6453d7c` fechado. Re-auditoria adversarial (2026-07-06): `6453d7c` era incompleto — no caminho de produção (LLM-first) o hook nunca chamava `checkOverride`, então o /eN expandido virava `source:llm` (sugestão descartável; só a doutrina executor-side salvava o comportamento) e a camada 0a de meta-comando derrotava o /eN em ambos os engines; fechado em `30f1fc6` (override determinístico no hook antes do bypass e do LLM; camadas invertidas no classifyPrompt; guardas literal+expandido vs colisão) |
| W1.3 | Contradição 4-vs-2 agentes na Research | `skills/Research/SKILL.md` + `StandardResearch.md` | contagens hardcoded herdadas; contradição introduzida no port | reconciliar números (fonte única vem em W2.5) | **feito** `afe61cd` — 10 correções em 3 arquivos; follow-up `729b5a1` — linha ACTIONS "2 researchers" e prosa binária do cross-check no mesmo template (miss da auditoria de medição) |
| W1.4 | Blocos "Legacy Pulse" de voz (~30 skills) | `skills/*/SKILL.md` | herdado do upstream (curl 31337 em 138 arquivos lá) | deletar (o próprio texto se declara superado por `pai_notify`) | **feito** — 580 linhas removidas de 30 skills; follow-up `e3ed1f2` — bloco optional byte-idêntico sobreviveu no AudioEditor sob o header antigo "## Voice Notification" (deleção era keyed no título) |
| W1.5 | Pin `deepseek-v4-flash-free` em validators | `validate-pai-installation.sh:497`, `test-behavioral.sh:181` | acomodação de custo (free tier), já despinada no runtime | validar "existe default centralizado", não o nome literal | **feito** `6b3e5a3` — checagem de propriedade via resolveClassifierConfig; follow-up `2cc6f2c` — shape-check provider/model (não-vazio era vacuous) + erro de toolchain distinto de erro do resolver |
| W1.6 | Heurísticas 404/length de qualidade web | `pai-hooks.js:1592-1624` | **port-added** (upstream só faz injection-scan) | deletar; logar só fatos de transporte | **feito** — injection-scan intacto, só contentLength no evento |
| W1.7 | Guardrail MUST-invoke-CreateSkill | `skills/CLAUDE.md` | herdado; cita incidente upstream cujo arquivo nunca foi vendorado | reduzir a convenção + mandato estreito no caso irreversível (private→public) | **feito** — mandato duro só em private→public |
| W1.8 | Rating implícito fabricado (rating=8/conf 0.95) | `pai-hooks.lib.js` detectPositivePraise | mecanismo herdado (`SatisfactionCapture.hook.ts`); constante sem incidente | remover score fabricado; manter `parseExplicitRating` | **feito** — evento qualitativo praise_detected; /rate N segue numérico |

## Mantidos deliberadamente (não são dívida — NÃO "consertar")

Piso determinístico de bash/secrets/egress e sandbox bwrap (incidente `rm -rf $HOME`, 2026-05-20); tier `alert` do corpus de padrões (é log, não decisão); scanner de injection em conteúdo externo (alert-only, defesa contra circularidade); pins de reprodutibilidade dos audits cross-vendor; fallback heurístico offline do classificador (camada 2, não primária). Racional completo: §5 do relatório.

## Fase 3 (probes e substitutos — a construir)

**W3.1 — Probes de aposentadoria.** A cada bump de modelo, rodar e comparar: (a) roteamento de skill/agente correto SEM keywords mágicas; (b) detecção de injection por leitura do modelo vs corpus; (c) self-selection de modo/tier vs golden set. Probe verde por 2 bumps consecutivos = PR de deleção agendado. Host natural: `pai-health.timer` + skill HarnessCalibration.

**W3.2 — Satisfação emitida pelo modelo (substituto real do W1.8; órfã até 2026-07-06 — PHASE1.md a nomeava sem linha aqui).** O eco de reflexão da doutrina (`v6.3.3.md` §reflection echo) manda o modelo preencher `implied_sentiment`/`satisfaction_prediction` `[1-10]` — observado vivo (2026-07-06) preenchido com `8` fixo nas duas entradas: precisão fabricada por prompt, mesma classe do rating=8 removido no W1.8. Mudança: campo de satisfação julgado pelo modelo com rubrica explícita (ou remoção dos campos do echo), e revisar as bandas congeladas de consumo (`MineReflections.md` `<=5 HIGH / 6-7 MEDIUM / 8-10 LOW`). **pendente**

## Fase 2 (estrutural — consolidar contratos, virar heurística→modelo)

| ID | Mecanismo | Local | Mudança | Status |
|---|---|---|---|---|
| W2.3 | Bloco CONSTITUTIONAL / STORY-1-8 duplicado em 10+ agentes | `opencode/agents/*.md` | dropar STORY-1-8 rígido; canonicalizar cap de 12 palavras; fence de drift | **feito** `cf4d96b` — suíte 543/0; teste agent-output-contract; calibração deixada não-commitada |
| W2.4 | Idioma capado en/pt em 2 normalizers + schema | `pai-hooks.lib.js`, `broker-lib.ts`, `notification-event.schema.json` | preservar qualquer BCP-47; schema geral | **feito** `47bfd83` — suíte 545/0; es-ES/fr preservados |
| W2.1 | Gates de keyword AgentGuard/SkillGuard (relevância) | `pai-hooks.lib.js` inspectAgentSpawn | dropar tabelas de keyword; manter só piso de recurso (fan-out cap) | **feito** `ebcd3ab` — 7 tabelas de keyword removidas (−493 linhas), SkillGuard aposentado inteiro (nada não-keyword restava), deny env-gated removido; fica só o fan-out cap (warn, nunca gate). Nota: o "precisa teste vivo" original era vacuous — os guards já eram advisory-only (deny default-off, SkillGuard nunca negava), remoção não muda roteamento; piso de segurança intocado 176/0 |
| W2.2 | Classificador de modo como gate (2º modelo) | `mode-classifier.lib.js`, `pai-hooks.js` | self-selection default; classificador → shadow (telemetria com `applied:false`, /eN e meta-comando seguem vinculando) | **mecanismo pronto, flip pendente** — `50b7cce` shippa `mode: gate\|shadow` (default gate, comportamento inalterado; fences comportamentais no hook real); eval-guarda estendido em `bfe0322` com os braços `tier-under` (under-correction de tier, a forma real do incidente) e `none` (self-selection sem bloco — o gate do flip, ship só com ≥80% no executor de produção). Baseline all-kinds medido (2026-07-06, openai/gpt-5.5, runs=1, concurrency 2): **under 24/24 · tier-under 7/10 (70%, warn) · over 22/23 · control 24/24 · none 47/47 (100%)** — gate de ≥80% no `none` passado com folga; tier-under 70% mostra a sugestão errada-para-baixo ANCORANDO o executor (subiu só até E2 em 3 casos E3+), i.e. no pior modo de falha do classificador a sugestão é ativamente nociva. Flip para shadow APROVADO pelos dados; aplicação = `"mode": "shadow"` no classifier.json da máquina (edição de config de runtime fica com o Principal). Depois: shadow verde por 2 bumps = aposentadoria (W3.1) |
| W2.5 | Corpora de trigger/routing nas skills; roster de agentes em prosa | `skills/*/SKILL.md` | manter USE WHEN; suavizar MANDATORY; roster em fonte única | **feito** `72bba0a` — Research perde o MANDATORY TRIGGER/keyword-map triplicado (vira seleção por intenção); teams em fonte única `Agents/Data/Teams/*.yaml` (o path que o SpawnTeam já lia mas NUNCA existiu; YAMLs mínimos — personalidade é composta pelo modelo, não congelada); roster de subagents vira ponteiro para `opencode/agents/*.md` (as cópias listavam Explore/Plan, que não existem no OpenCode) |
| W2.6 | Notificação + execution-log emitidos por prompt | 44 skills + hook | mover emissão pro runtime | **feito** `c072d36` — execution.jsonl agora é runtime-owned (tool.execute.after; `source:'runtime'`; campo `workflow` removido — era auto-reportado); 44 blocos echo removidos keyed no CONTEÚDO (lição do W1.4), 2 homônimos legítimos intactos; round-trip de schema no hook real + fence de árvore contra reintrodução. Evidência viva que motivou a prioridade: skill transcreveu o template com timestamp 00:00:00 (2026-07-06). notifications.jsonl já era runtime-emitido |
| W2.7 | Coreografia por minuto / IMMUTABLE nos agentes | `opencode/agents/*.md` | trocar por meta funcional; suavizar absolutos | **feito** `0eeda0e` — 10 rituais de startup viram carga funcional com o porquê; curls 31337 com voice_id pinado removidos; Nine Articles IMMUTABLE→strong defaults com desvio documentado; micro-cycle por minuto→meta por ciclo; Ultrathink pins→profundidade proporcional ao blast radius; bug real da temperatura do Anvil (0.3 vs 1 forçado) corrigido; 2 paths mortos consertados. KEEPs intactos (permission blocks, model pins, Cato/Arthur read-only, contrato de output W2.3). Flagged sem mudar: `--sandbox danger-full-access` do CodexResearcher (item de segurança próprio) |
| W2.8 | Prose bands do parameter-schema; piso HARD de thinking; whitelists | `PAI/ALGORITHM/*` | afinar bands; piso relaxável; descobrir paths | **feito (parcial deliberado)** `bd746b6` — v6.3.4: piso de thinking HARD→soft (show-your-math; contra-evidência = baseline W2.2 47/47); bands do parameter-schema anotadas como defaults interpoláveis. **Enumeração fechada + phantom rule MANTIDAS como KEEP**: a evidência de campo da v6.3.0 (wallpapering medido; ponteiros vazam vocabulário sob pressão) segue de pé e o baseline não mediu honestidade de capability — aposentadoria só via probe W3.1 |
| W2.9 | Counts mágicos nos validators; **fonte duplicada** `PAI/bin/*` vs `opencode/bin/*` | `validate-pai-installation.sh`, `test-behavioral.sh` | checagem de contrato, não de contagem; UMA fonte por validator | **feito** `02a816b` — agents via manifest (count + nomes), schemas via contrato explícito por stream (7 nomes), check '>50 linhas' removido (drift-check subsume); repo `PAI/bin/` DELETADO (10 arquivos; o espelho stale que mordeu 2× em 2026-07-06), installer popula `\$PAI_DIR/bin` só de `opencode/bin`; fence de higiene |
| W2.10 | `PAI_META_COMMAND_SIGNATURES` — roteamento por substring literal de templates (violação #7 do relatório, órfã até 2026-07-06) | `mode-classifier.lib.js:166` | keyar o bypass no agente resolvido ou em registry de templates exatos. Defeito vivo confirmado: a assinatura do /classifier casa o template do /interview → sugestão NATIVE conf 0.95, contra a exclusão declarada no próprio comentário | **feito** `26c568b` — registry de heads exatos de template (âncora `^` por comando, match no head da mensagem); /interview não colide mais (fence com o template real); menção mid-prompt não dispara; defeito irmão (0a derrotava /eN) fechado em `30f1fc6` |
| W2.11 | Context Routing Table ~48 linhas topic→path, já driftando (violação #24 do relatório, órfã até 2026-07-06) | `PAI/CLAUDE.md` §Context Routing | descobrir paths via filesystem/registry; manter só a curadoria que o filesystem não expressa (canonical-vs-legacy) | **feito** `de25c02` — 12+ paths mortos verificados (schema TELOS upstream inteiro incluso, contradizendo o LifeOsSchema.md que a própria tabela linkava; rows roteando leitura para paths zero-access); substituída por convenções de descoberta + 7 rows de curadoria real; doc-integrity e validator verdes |
| W2.12 | Census `PAI/TOOLS/` — a camada implementada de memória/aprendizado ficou FORA do inventário original (auditoria adversarial 2026-07-06). Padrões-alvo em densidade máxima: `RelationshipReflect.ts` regexes de evidência + deltas mágicos (+0.02/−0.05/+0.10/−0.20, limiar 0.15) **mutando confidences persistidas em USER/OPINIONS.md** (pior que W1.8 — a fabricação persiste); `SessionHarvester.ts:268` confidence fabricada por fórmula; `MigrateScan.ts` tabela de ~40 keywords + confidence mágica (mitigado por fila de aprovação); `LearningPatternSynthesis.ts` recomendações enlatadas por bucket; `KnowledgeHarvester.ts` roteamento de domínio por keyword | auditar cada tool com o gate de 3 perguntas; prioridade 1 = RelationshipReflect (bloquear escrita não-dry-run até o veredito) | **P1 feito** `149a35b` — escrita não-dry-run sai `blocked` exit 2 antes do availability check (dry-run segue disponível; manifest atualizado); census + veredito das demais tools pendente |
